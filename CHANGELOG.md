# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/)。

## [1.4.2] - 2026-09-08

### Configuration

- 公开通用版不预置实际群号和群名，保留贵港/南宁及当前线路、货物、价格规则，首次使用通过页面添加自己的群。
- 测试群号使用合成标识，与安装默认配置分离；不包含个人聊天、数据库、QQ登录态或凭据。
- 新安装优先D盘（没有D盘时使用当前用户目录），NapCat组件跟随本系统安装目录；旧安装升级继续保留原配置和数据。
- 新增无需Markdown阅读器的“快速开始.html”，更新纯文本、手动年度、开始/停止采集、出图及升级说明。

### Fixed

- 提升绘图依赖Pillow下限至12.3.0，避开发布前依赖审计检出的旧11.3.0安全公告；关闭OCR不等于无需维护绘图依赖。
- 修复“打开管理页面”在后台未启动时只等待的问题：先启动监督程序，再等待页面就绪。
- 修复打包冒烟测试的带空格路径参数；安装器不再用全局互斥量阻止安装到另一目录，仍由文件占用检测保护升级。不要同时启动多份系统。

### Verification

- 发布前后分别安装与全链路验收；下载资产按SHA-256与发布前被测文件对比，具体结果以Release附带验收报告为准。

## [1.4.1] - 2026-09-08

### Fixed

- 修复“到福州大板181”等省略始发地的文字报价，使用对应群默认始发地；未知显式始发地仍拒绝，贵港/南宁互斥规则不变。
- 拒绝“71 1元/吨”等未说明用途的小数字/拆分价格，防止忽略短数字后保存错误报价。
- 区分“120 车长9.6米 / 吨位35”与数量单位，避免运价被误过滤；保留明确吨位、车长、装卸数量和费用说明。

### Configuration

- 默认关闭OCR，纯图片消息直接跳过、不下载图片；附图文字仍按文本处理。默认模板同步为纯文本，统计PNG/Excel输出不变。
- 不重新解析或清理旧报价，不修改群/线路/年度和手动采集开关状态。

## [1.4.0] - 2026-09-08

### Added

- 全局“停止采集 / 开始采集”：持久化采集开关，停止后先排空已收到队列；暂停期间新消息不入库，恢复不补采暂停期间的消息，重启保持状态。
- 数据与报表新增“生成当日图表”：按群、报价生效日期、始发地/目的城市筛选，后台从固定快照输出PNG和配套Excel。
- 日图标注日期、群、均价口径、报价数及生成时间；多线路分页出图；无有效报价不生成误导性的空图。
- 新增/删除报价或修改影响统计的规则后提示重新生成；保留历史快照，不静默改写已分享图片；失败/中断任务提示重试。
- 新接口沿用本机Origin与CSRF校验；健康接口区分连接在线与手动停止采集。

### Notes

- 出图、日常采集控制、年度归档三者独立；当前手动年度继续自动更新Excel/CSV，PNG按用户按钮生成。
- 不改变现有群/线路/历史数据。本版本不包含上轮验收发现的多行OCR换行修复，该问题仍需单独处理。

## [1.3.2] - 2026-09-08

### Added

- 新增QQ群级别“不统计的始发地”：贵港示例群默认排除南宁，南宁示例群默认排除贵港；测试群不额外限制。
- 消息别名先归为标准始发地再判断，未写始发地仍用本群默认值；只过滤新增接收记录，不删除或重新分类历史报价。
- 群与线路管理支持查看、修改、清空过滤列表；校验不存在的始发地和排除本群默认始发地等冲突。旧客户端省略字段时保留已有过滤规则。
- 后续新安装模板同步两群过滤设置；新增群留空时不额外过滤，需在页面自行配置。

## [1.3.1] - 2026-09-08

### Fixed

- 拒绝负数/疑似负价，避免负号丢失后转成正报价入库；保留正常正数区间价。
- 按用户要求不采集按方/立方米计价，支持元/方、每方、元/立方米、元/m³等常见写法；不把货量“30方”与吨价混淆。
- 在完整消息及价格提取入口检查，避免线路整理提前丢弃负号或计价单位；实时文字、OCR识别后的文字和手动文本导入共用规则。
- 同条消息逐行处理，合格吨价继续采集，不合格行保留明确原因，不进入报价明细或均价。

### Notes

- 不清理已存在的历史报价，不迁移调试期武鸣/南宁旧分类，不改变当前群/线路配置。

## [1.3.0] - 2026-09-08

### Added

- 新增“年度归档”页面：用户手动启用统计年度、预览并结束年度、等待后手动开启下一年度，统一控制全部已配置QQ群。
- 关账时保存固定SQLite快照及各群的Excel、CSV，包含大小类日/周/月/年度汇总；失败可从原快照重试，重启继续未完成归档。
- 暂停期原始记录保留可查询，但不计入年度、不自动补入下一年度；新年度开启时隔离已入库数据。
- 当前日均及手动年度报表按当前周期过滤；保留原春节报表，首次启用前保持原有模式不变。
- 状态版本校验、二次确认和跨进程归档互斥；有排队消息时暂缓年度切换，防止跨边界混入数据。

### Notes

- 年度按生效日期的“天”划分，不允许日期重叠；截止日为今天时，下一年度最早明天开启。
- 归档不是删除；归档完成后不会随原始数据删除或规则调整自动改写。首次启用由用户操作，不随升级自动开启。

## [1.2.1] - 2026-09-08

### Fixed

- 根据本地NapCat脱敏文字样本修正价格区间被截取为日期的问题，区分车长、吨位与运价。
- 支持全角数字、全角斜杠及数字间空格；正确提取写在“到”字前的已配置货物名称。
- 多个关系不明的价格、加价表达及缺少月份的“22号”等不再猜测，保留未识别提示。
- 保持线路范围、货物配置、整车价阈值和统计口径不变，不自动重导入历史消息。

## [1.2.0] - 2026-09-08

### Added

- 系统新增“数据与报表”：按群、日期、线路、货物小类、报价范围、关键词查询全部历史报价，支持正式/待生效/不合格分类和分页。
- 直接查看大小类日均统计、Excel工作表、CSV和PNG走势图，并下载原报表；保留现有输出目录和周期统计口径。
- 单条或勾选批量删除，服务端预览确认、冲突检测、删除记录留存与同内容重新导入拦截。
- 删除后同步重建Excel、CSV及图表，持久化待更新状态，服务重启后继续同步。

### Fixed

- 数据库清空后不再从旧TXT自动恢复已删除报价。
- 某历史春节年度的最后一条报价删除后，对应大小类归档也重新生成空统计。

## [1.1.2] - 2026-08-20

### Fixed

- NapCat稳定启动器现在根据自身安装位置解析版本目录，并在调用官方启动脚本前切换到实际 `napcat` 目录，修复官方脚本依赖 `%cd%` 时错误查找根目录下 `NapCatWinBootMain.exe` 的问题。
- 增加稳定启动器 `--check` 命令行回归验证，实际确认安装路径、工作目录、ASCII内容和CRLF换行均正确。
- 发布脚本的NapCat缓存替换改为兼容Windows PowerShell的两参数文件移动方式。
- 发布包冒烟测试改用ASCII内容标记定位NapCat安装入口，避免Windows PowerShell 5.1因中文代码页误判文件缺失。

## [1.1.1] - 2026-08-20

### Fixed

- 将Windows发行包中的批处理脚本强制转换为ASCII内容和CRLF换行，修复 `cmd.exe` 将 `setlocal`、`powershell.exe` 等命令截断的问题。
- NapCat安装入口改用ASCII脚本文件名，并增加批处理格式与实际 `--check` 回归验证，避免中文Windows代码页导致脚本路径解析失败。

## [1.1.0] - 2026-08-20

### Added

- Windows Release原样内置经SHA-256校验的NapCatQQ Shell Windows Node官方包、完整许可和一键解压安装入口，不包含腾讯QQ。
- 一次性 `start` 启动入口和 `/api/health`：按实际配置端口等待服务就绪后自动打开实时状态页。
- 兼容NapCat `message_sent` 自身群消息事件。
- 每个QQ群独立SQLite持久收件箱、串行消息工作线程和有界内存唤醒队列。
- 每群独立Excel增量工作线程，以及受全局锁保护的全量构建。
- 正式识别、待生效、不合格数据的实时明细接口和页面。
- WebSocket心跳、断线重连次数、最近链路事件和队列积压指标。
- 管理页可配置重连、心跳、Excel批量追加与汇总刷新间隔。
- 管理页可单独设置手动文本模式默认始发地。
- 配置管理完整性测试和真实WebSocket配置重载测试。
- 配置硬中断恢复、接口安全、持久队列重放和监督进程范围测试。
- “群与线路管理”页的数据生命周期配置，以及每群自动清理、备份轮转和运行状态反馈。
- 生命周期支持全局默认模板、新增群自动继承和可选群专属覆盖；不同群按各自维护间隔独立调度。

### Changed

- 启动器依赖自检现在显式检查 `lunardate`，避免全新环境缺少春节年度依赖时误报可运行。
- 启动与管理快捷方式不再依赖固定8765端口和5秒延时；监督器可定向恢复连续90秒接口无响应的采集器。
- Excel由逐消息全量重建改为批量追加明细，并低频校准统计汇总。
- 状态文件写入改为短间隔合并，页面内存状态仍实时更新。
- 未识别消息保存逐行具体拒绝原因。
- 群输出目录改为基于群号的稳定目录 `QQ群_<群号>`；自动迁移单个旧版“群名_群号”目录，群改名不再切断历史数据。
- 页面保存配置后先停止接收、排空群队列和Excel队列，再在同一进程内重新加载，无需依赖监督程序恢复。
- Matplotlib固定使用无界面 `Agg` 后端，避免后台统计线程触发GUI异常。
- 管理接口增加Host/Origin/CSRF、配置版本冲突和Token脱敏；URL、端口和字段校验改为严格模式。
- 双配置文件增加事务日志、generation恢复和50份备份上限；重载排空超时不再启动重叠会话。
- 源码模式停止命令只匹配本项目进程，不再按通用Python可执行文件结束其他程序。

## [1.0.0] - 2026-08-10

### Added

- NapCatQQ / OneBot 11 指定QQ群实时采集与按群独立输出。
- 始发地、目的地、货物和多价格提取，支持Windows图片OCR。
- 同内容跨发布人查重，以及未来缓存共同查重。
- 明天、后天、明确日期和星期报价的延迟生效。
- SQLite增量存储、未识别队列、每日备份、日志轮转和可信时间。
- 日均Excel数据及周、月、年CSV和价格走势图。
- 本机群与线路管理页面、配置备份和自动应用。
- Windows免Python便携版与Inno Setup安装版。
